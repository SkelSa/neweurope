##This file will is used for some hidden events. Only a few are here.
add_namespace = ne_hidden

country_event = {
	id = ne_hidden.1
	title = ne_hidden.1.t
	desc = ne_hidden.1.d

	hidden = yes

	fire_only_once = yes

	trigger = {
		SOV = {
			has_idea = stalins_death
		}
	}

	mean_time_to_happen = { days = 105 }

	immediate = {
		hidden_effect = {
			SOV = { remove_ideas = stalins_death }
		}
	}

    option = {
        name = ne_hidden.1.a
    }
	
}